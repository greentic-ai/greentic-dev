## {{component_pascal}} Component

This component is generated by `cargo xtask new component {{component_name}}`.

### Build

```bash
cargo component build --release
```

### Validate locally

```bash
cargo xtask validate --path .
```

### Configuration schema

See `schemas/v1/config.schema.json` for the JSON Schema that the Greentic runner expects.
