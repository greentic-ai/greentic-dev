## {{component_pascal}} Component

This component is generated by `greentic-dev component new {{component_name}}`.

### Build

```bash
cargo component build --release --target wasm32-wasip2
```

### Validate locally

```bash
greentic-dev component validate --path .
```

### Configuration schema

See `schemas/v1/config.schema.json` for the JSON Schema that the Greentic runner expects.
